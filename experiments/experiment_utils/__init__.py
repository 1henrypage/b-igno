"""Shared utilities for posterior inference experiments."""

from ._metrics import (
    crps_ensemble,
    compute_calibration,
    ci_width_95,
    chi2_ppc,
    nll_score,
    compute_prior_predictive,
    compute_unconditional_prior_metrics,
    compute_standard_metrics,
    compute_piecewise_metrics,
    bernoulli_nll_score,
    compute_bootstrap_ci_block,
    ESS_WARN_THRESHOLD,
    ESS_FAIL_THRESHOLD,
    RHAT_WARN_THRESHOLD,
    RHAT_FAIL_THRESHOLD,
    DIV_WARN_FRAC,
    DIV_FAIL_FRAC,
    mcmc_reliability_flag,
    bootstrap_metric_ci,
    bootstrap_metric_difference_ci,
    compute_per_chain_metrics,
    compute_metric_convergence,
)

from ._mcmc import (
    recommended_nuts_config,
    tune_sigma,
    compute_sigma_from_map,
    select_sweep_sigma,
    make_nf_reparameterized_model,
    run_mcmc,
    extract_mcmc_diagnostics,
)

from ._plotting import (
    plot_metric_convergence,
    plot_field_comparison,
    plot_calibration,
    plot_posterior_gallery,
    plot_posterior_predictive,
    plot_trace,
    plot_metrics_table,
    plot_rho_sweep,
    plot_sharpness_calibration_tradeoff,
    plot_std_comparison,
    plot_metrics_comparison_table,
    plot_std_comparison_generic,
    plot_metrics_comparison_table_4way,
    plot_calibration_overlay,
    plot_sweep_summary_table,
    plot_noise_sweep,
    plot_sensor_sweep,
    plot_std_multi_panel,
    plot_uncertainty_investigation,
    plot_eit_ground_truth,
    plot_eit_observation_data,
    plot_burgers_field_comparison,
    plot_burgers_posterior_gallery,
    plot_burgers_uncertainty_investigation,
    compute_error_std_correlation,
    plot_burgers_std_comparison,
    plot_burgers_std_multi_panel,
    plot_physics_benefit_sweep,
    plot_delta_metrics_sweep,
    plot_physics_benefit_comparison,
    _use_science_style,
)

from ._serialization import (
    format_significance_table,
    build_mcmc_result,
    build_laplace_result,
    build_prior_result,
    build_unconditional_prior_result,
    save_experiment_result,
    load_experiment_result,
    load_cross_seed_results,
    cross_seed_metric_summary,
    cross_seed_consistency_test,
    cross_seed_comparison_robustness,
    format_cross_seed_table,
    print_cross_seed_summary,
    print_per_chain_table,
    print_dimension_diagnostics,
)

from ._setup import (
    load_problem,
    get_nf_mode,
    make_log_prior,
    make_gaussian_log_likelihood,
    make_numpyro_model,
    make_numpyro_model_physics,
    setup_observations,
    sample_unconditional_prior,
    decode_posterior_batched,
    run_map_estimation,
    compute_sigma_from_map_residual,
    add_noise_snr_with_sigma,
    decode_initial_condition_burgers,
    make_igno_loss_fn,
)
